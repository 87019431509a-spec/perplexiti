# 🤖 NEUROCOMMENT - Система нейрокомментинга Telegram

Консольный Python-сервис для автоматического комментирования постов в Telegram-каналах через Telethon.

## 🚀 Возможности

- **Нейрокомментинг** - автоматические комментарии с GPT генерацией
- **Персонаж** - девушка-анимешница-музыкантша с живым стилем
- **Мульти-аккаунт** - одновременная работа с несколькими аккаунтами
- **Два пула аккаунтов** - отдельные для комментирования и прогрева
- **Антибан система** - SAFE/NORMAL режимы, задержки, лимиты
- **Вероятность комментирования** - настраиваемый % пропуска постов
- **Мониторинг удалений** - проверка через peer_id с настраиваемым интервалом
- **Прогрев** - читение, реакции, вступления для снижения риска бана
- **TData конвертация** - автоматическая конвертация tdata в сессии
- **Отдельные окна логов** - для комментирования и прогрева (Windows)

## 📁 Структура папок

```
project/
├── каналы/                 # Списки каналов для комментирования (.txt)
├── tdata_комменты/         # TData аккаунтов для комментинга (tdata1/, tdata2/...)
├── sessions_комменты/      # Сессии Telethon для комментинга
├── прокси_комменты/        # Прокси для комментинга (.txt)
│
├── каналы_прогрев/         # Каналы для прогрева (.txt)
├── чаты_прогрев/           # Чаты для прогрева (.txt)
├── tdata_прогрев/          # TData аккаунтов прогрева (tdata1/, tdata2/...)
├── sessions_прогрев/       # Сессии Telethon прогрева
├── прокси_прогрев/         # Прокси для прогрева (.txt)
│
├── logs/                   # Логи системы
│   ├── comments.log        # Логи комментирования
│   └── warmup.log          # Логи прогрева
│
├── data/                   # База данных
│   └── bot.db
│
├── main.py                 # Точка входа
├── config.yaml             # Конфигурация
└── requirements.txt        # Зависимости
```

## 🔧 Установка

```bash
cd project
pip install -r requirements.txt
```

## ⚙️ Настройка

1. Получите API credentials на https://my.telegram.org
2. Отредактируйте `config.yaml`:
   - `telegram.api_id` и `telegram.api_hash`
   - `gpt.api_key` для OpenAI
3. Поместите данные:
   - `.session` файлы в `sessions_комменты/` и `sessions_прогрев/`
   - или tdata папки в `tdata_комменты/` и `tdata_прогрев/`
   - каналы в `каналы/` (файлы .txt, по одному @каналу на строку)
   - прокси в `прокси_комменты/` (формат: host:port или user:pass@host:port)

## 🚀 Запуск

```bash
python main.py
```

При запуске на Windows автоматически откроются дополнительные окна:
- **Окно 1** - Главное меню управления
- **Окно 2** - Логи комментирования (tail comments.log)
- **Окно 3** - Логи прогрева (tail warmup.log)

## 📋 Главное меню

```
1. Запустить комментирование
2. Остановить комментирование
3. Запустить прогрев
4. Остановить прогрев
5. Статус
6. Настройки
7. Проверка прокси
8. Выход
```

## ⚙️ Меню настроек

```
1. Режим (SAFE/NORMAL)
2. Вероятность комментирования (0-100%)
3. Задержки
4. Лимиты
5. GPT
6. Прокси
7. Санкции
8. Мониторинг удалений (интервал в секундах)
9. Сохранить конфиг
0. Назад
```

## 🎭 Персонаж (промпт)

Девушка-анимешница-музыкантша:
- Короткие комментарии 3-5 слов (макс 10)
- Иногда делает грамматические ошибки
- Использует сленг: "кста", "оч", "збс", "норм"
- **СТРОГО ЗАПРЕЩЕНО**: война, политика, геополитика
- При постах о войне/смерти - только грустный смайл 😢
- Может вернуть SKIP для пропуска неинтересного поста

## 📊 Статус показывает

- Сервисы: работает/остановлен
- Аккаунты: sessions, tdata (ждут конверт), активные
- Каналы: всего, активные, в спячке, отключены
- Прокси: всего, ок, bad (для каждого пула)
- Комментирование: отправлено, ошибок, пропущено
- Мониторинг: проверено, подтверждено, удалено
- Проблемы одной строкой

## 🛡️ Режимы работы

### SAFE Mode
- Увеличенные задержки (x2)
- Сниженные лимиты (/2)
- Автопауза при FloodWait
- Рекомендуется для новых аккаунтов

### NORMAL Mode
- Стандартные задержки
- Полные лимиты

## ⚖️ Санкции

- **1 удаление** → канал уходит в спячку
- **2+ удалений** → канал отключается
- В SAFE режиме аккаунты не банятся, только паузятся

## 🔍 Мониторинг комментариев

- Проверка через `get_messages(peer_id, comment_id)`
- `peer_id` - ID discussion group (обязательно)
- FloodWait, RPC ошибки НЕ считаются удалением
- Удаление = `get_messages` вернул None/пусто
- Интервал проверки настраивается в секундах

## 🔥 Прогрев

Уровни 0-100:
- **0-10**: только чтение
- **10-25**: чтение + реакции
- **25-50**: + вступления в каналы
- **50-75**: + комментарии
- **100**: полный функционал

### Исправление Invalid Peer

- Боты автоматически пропускаются
- Невалидные peer обрабатываются корректно
- Ошибки логируются без падения сервиса

## 📝 Формат файлов

### Каналы (каналы/*.txt)
```
@channel1
channel2
t.me/channel3
```

### Прокси (прокси_комменты/*.txt)
```
host:port
user:pass@host:port
socks5://user:pass@host:port
http://host:port
```

## 📄 Лицензия

MIT
